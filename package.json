{
  "type": "module",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "e2e"
  ],
  "scripts": {
    "build:client": "yarn workspace wake-ghost-website-client build",
    "dev": "yarn workspace wake-ghost-website-server dev",
    "dev:debug": "yarn workspace wake-ghost-website-server dev:debug",
    "heroku-postbuild": "yarn workspace wake-ghost-website-client build",
    "start": "yarn workspace wake-ghost-website-server start",
    "dev:cypress": "yarn workspace wake-ghost-website-server dev:e2e",
    "e2e:open": "yarn workspace wake-ghost-website-e2e e2e:open",
    "e2e:run": "yarn workspace wake-ghost-website-e2e e2e:run"
  },
  "engines": {
    "node": "^14.15"
  }
}
